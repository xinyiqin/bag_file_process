# Core dependencies
numpy>=1.24.0
opencv-python>=4.8.0
labelme>=5.2.1
timm>=0.9.0

# Optional:
# - Install ROS 1 (rosbag, cv_bridge) or an equivalent environment if you need to read .bag files directly.

